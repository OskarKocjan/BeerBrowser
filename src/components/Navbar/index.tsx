import React from 'react';
import { StyledNavBar } from './styled';
import { StyledNavLink } from './styled';

const Navbar = () => {
    return (
        <StyledNavBar>
            <StyledNavLink>lorem ipsum</StyledNavLink>
        </StyledNavBar>
    );
};

export default Navbar;
